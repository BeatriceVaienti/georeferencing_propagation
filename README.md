# georeferencing_propagation
Repository for the propagation of the Ground Control Points from georeferenced maps to non-georeferenced maps. The repository contains the code needed to apply the methodology having a target map and at least one georeferenced map. The code is written in Python and uses SuperPoint and SuperGlue to extract and match keypoints between the maps. 

